using Gtk 4.0;
using Adw 1;

template $MainWindow : Adw.ApplicationWindow {
  default-width: 800;
  default-height: 500;
  title: _("Chatenium");

    Adw.ToolbarView {


    Adw.ToastOverlay toast_overlay {
      Adw.ViewStack view_stack {}
      }
    }
  }